// User blueprint
export class User {
    // tslint:disable-next-line:max-line-length
    constructor(public user: string, public user_login: string, public img_link: string, public user_link: string, public user_bio: string) { }
}
